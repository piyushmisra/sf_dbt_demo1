sources:
  - name: health_app_src
    database: raw
    tables:
      - name: meds
        columns:
          - name: id
            data_type: number
          - name: med_name
            data_type: varchar

      - name: patients
        columns:
          - name: id
            data_type: number
          - name: first_name
            data_type: varchar
          - name: last_name
            data_type: varchar

      - name: prescriptions
        # loaded_at_field: created_at
        # freshness:
        # warn_after: {count: 24, period: hour}
        columns:
          - name: med_id
            data_type: number
          - name: patient_id
            data_type: number
          - name: dosage
            data_type: varchar
          - name: frequency
            data_type: varchar
          - name: date_prescribed
            data_type: date
          - name: first_dosage_date
            data_type: date
          - name: end_dosage_date
            data_type: date
